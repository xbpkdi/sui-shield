"use client";

import { CursorFx } from "@/components/effects/CursorFx";

/** Global premium UX effects (10K checklist) — desktop only, non-blocking. */
export function UiEffects() {
  return <CursorFx />;
}